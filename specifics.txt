chmod -R 777 *
changes the permission of all present files and directories and their existent subfolders


path-to-qutebrowser/.venv/bin/python3 -m qutebrowser "$@"
/home/isabel/Documents/qutebrowser/.venv/bin/python3 -m qutebrowser "$@"

to add a ip for NASTY message from ssh protocol
ssh-keygen -R 192.168.3.10
git@github.git@github.com:albertpuente/herding-spikes.git
192.30.253.113 = github.com
for the spike- detection code:
git clone -b detection-cpp https://github.com/martinosorb/herding-spikes.git

for the spike- detection code:
git clone -b detection-cpp https://github.com/martinosorb/herding-spikes.git
to add a new user ubuntu style
sudo useradd -m HELGA
sudo passwd HELGA
sudo useradd -m -s /bin/bash HELGA
sudo usermod -s /bin/bash HELGA

to add the user of interest to allow root privileges
edit:
/etc/sudoers
or edit /etc/group

python libraries: pip install ''
pip show name-library-python
matplotlib needs more no-python stuff. must be installed form package manager and not form pip and such.
important for python:
install python-dev
python3-dev

check for compilers:
gentoo: eix -Is compiler
ubuntu:dpkg --list | grep compiler

sudo apt-get install binutils build-essential m4 autogen bison flex
sudo apt-get install g++

 systemctl reboot -i
 for rebooting ignoring logged users

ssh -X 

tar gz
tar -zxvf
tar bz2
tar -jxvf
